# Open Design Helper

This directory is kept as a regular project folder so the repository can be cloned,
tested, and archived without requiring an undeclared Git submodule.

The diploma MVP does not depend on this folder at runtime.
